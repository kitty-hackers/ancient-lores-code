name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    run-on: ubuntu-latest
  
    steps:
      - uses: actions/checkout@v2

      - name: build
        run: echo "Build"